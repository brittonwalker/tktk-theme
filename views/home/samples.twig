<div class="container mb-14">
	<p class="text-5xl block">Templates</p>
	<p class="mt-3 text-base text-gray-500 sm:mt-5 sm:text-lg md:mt-5 md:text-xl lg:mx-0 lg:max-w-[800px] my-10">TKTK includes minimally styled templates for posts, pages, and archives to help you visualize content from the start. These templates are the basis for any WordPress site, easy to read, extend, and can quickly become your own.</p>
	<div class="grid grid-cols-3 gap-4">
		{% for item in menu.sample.get_items %}
			<a class="sample" target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a>
		{% endfor %}
	</div>
</div>